새 기능을 추가한다. 사용자가 기능을 설명하면 아래 순서로 진행해줘.

1. **현재 코드 파악**: 관련된 `screening.py`, `screening_core.py`, `us_data_client.py`, `cache.py` 등 확인
2. **계획 제시**: 변경이 필요한 파일과 내용을 먼저 설명하고 사용자 승인 받기
3. **구현 순서**:
   - 데이터 소스 / 캐시 스키마 변경이 필요하면 먼저 처리
   - 데이터 API (agent4 담당) → 백엔드 로직(agent3) → UI(agent2) 순서
   - 필터 조건 변경 시 기본값과 UI 슬라이더/체크박스 동시 업데이트
4. **완료 후**: CLAUDE.md 및 `.claude/plans/PLAN.md` 자동 업데이트

스타일 규칙:
- 다크 테마: 배경 `#0e1117`, 카드 `#1e2130`
- 수익(상승): 빨간색 `#ff4b4b`, 손실(하락): 파란색 `#1a9cff` (한국식)
