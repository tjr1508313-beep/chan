# 비밀번호 잠금 설정.
# 이 파일을 `.streamlit/secrets.toml` 로 복사한 후 비밀번호를 설정하세요.
# `.streamlit/secrets.toml` 은 .gitignore 처리되어 commit 되지 않습니다.
#
# 비번 미설정(빈 문자열) 또는 secrets.toml 자체가 없으면
# 잠금 화면은 표시되지 않습니다 (로컬 개발 편의).
#
# 배포 환경(Oracle Cloud 등)에서는 반드시 설정하세요.

app_password = ""
